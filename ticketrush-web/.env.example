# Base URL for user_api.
#
# Recommended (dev): keep default proxy from Vite and use a relative URL.
# Recommended (docker): keep relative URL so Nginx can proxy to the user_api service.
VITE_USER_API_URL=/user-api
VITE_GOOGLE_CLIENT_ID=
VITE_FACEBOOK_APP_ID=
VITE_OAUTH_REDIRECT_BASE_URL=
